services:
  uptime-atlas:
    build: .
    ports:
      - "8080:8080"
    environment:
      UPTIME_ATLAS_SESSION_SECRET: "change-me"
      UPTIME_ATLAS_ADMIN_USER: "admin"
      UPTIME_ATLAS_ADMIN_PASSWORD: "change-me"
    volumes:
      - ./data:/app/data