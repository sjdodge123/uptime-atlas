# Tasks

## Planned
- Confirm Pelican schedules schema and full cron/timezone behavior.
- Implement robust cron parsing (step values, advanced expressions).
- Add calendar zoom control (density slider).
- Add hover tooltip for calendar events in addition to click popover.
- Improve calendar event detail modal (full schedule metadata).
- Wire Discord OAuth to widget for read/post/pin.
- Add Discord message scheduling based on calendar events.
- Implement WOL automation with configurable offsets.
- Add background scheduler/worker for automation tasks.
- Add per-user notification preferences.

## In Progress
- None.

## Completed
- Base FastAPI app + Dockerfile/compose.
- Admin login/setup and session auth.
- Role-based access (root/admin/user).
- OAuth login (Google/Discord/Steam) with allowlists.
- Calendar month view with source filters.
- Schedule creation modal for Pelican.
- Profile page: timezone dropdown + password change.